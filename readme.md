# gh pages for quickemcee

A Python library to quickly set up MCMC scripts based on the `emcee` package. Probably will be used in future versions of my package `tsaopy`.

https://sofia-scz.github.io/quickemcee/
